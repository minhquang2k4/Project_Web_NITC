// const mongooes = require('mongoose');

// const connect = async () => {
//     try {
//         await mongooes.connect('mongodb://localhost:27017/shortUrl');
//         // mongodb://localhost:27017/ -> đường dẫn đến db
//         // shortUrl -> kết nối vào db tên là shortUrl (nếu chưa có thì sẽ tạo)
//         console.log('Database connected');
//     } catch (error) {
//         console.log('Database connection failed');
//     }
// }

// module.exports = {connect};